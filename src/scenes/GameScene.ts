import Phaser from 'phaser';

export class GameScene extends Phaser.Scene {
  constructor() {
    super({ key: 'GameScene' });
  }

  create(): void {
    // Implemented in Phase 2-4
  }

  update(): void {
    // Implemented in Phase 2-4
  }
}
